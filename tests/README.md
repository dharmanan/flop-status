# Test Layer

Tests will be added before any milestone is called complete.

Planned areas:

* `crypto/`
* `trials/`
* `receipts/`
* `integration/`

The first acceptance target is one complete vertical slice that creates a challenge, verifies a DID signed result, persists the result, issues a server signed receipt and verifies that receipt from a separate browser context.

No tests are implemented yet.
