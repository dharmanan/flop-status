# Application Layer

This directory will contain the human UI and HTTP API routes after implementation begins.

Core product behavior must remain downstream of the domain modules in `lib/`.

Planned areas:

* `api/`
* `agent/`
* `trials/`
* `verify/`

No application code is implemented yet.
